---
interface Props {
  title: string;
  variant?: "page" | "article";
}

const { title, variant = "page" } = Astro.props;
---

<h1
  class:list={["page-title", { "page-title--article": variant === "article" }]}
>
  {title}
</h1>
